# http1 & http2 binding 
bind = ["127.0.0.1:9810"]
# http3 quick binding
quick_bind = ["127.0.0.1:9810"]
# http path prefix for codex
root_path = ""
# This can be a number or "auto" for Codex to guess a good value.
# https://github.com/ajslater/codex#bulk-database-updates-fail
max_import_batch_size = "auto"
